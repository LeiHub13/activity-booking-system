<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.activitybookingsystem.mapper.RoleMapper">
   <select id="selectByRoleCode" resultType="com.example.activitybookingsystem.entity.Role">
       select id, role_code, role_name
       from role
       where role_code = #{roleCode}
   </select>
   <select id="selectRoleCodesByUserId" resultType="java.lang.String">
      select r.role_code
      from user_role ur
      join role r on ur.role_id = r.id
      where ur.user_id = #{userId}
   </select>
</mapper>